# Isaac - Docker Environment Configuration
# Copy this file to .env and update values before running docker compose up

# --- Required ---

# Encryption key for sensitive data (generate with: python -c "import secrets; print(secrets.token_urlsafe(32))")
# If not set, one will be auto-generated on first run (stored in container, lost on recreate)
SECRET_KEY=change-me-to-a-random-string

# --- Location & Timezone ---

TIMEZONE=America/New_York
LATITUDE=28.9134
LONGITUDE=-82.0938

# --- Optional: Weather ---

# Ambient Weather Network API keys (get from ambientweather.net/account)
# AWN_API_KEY=
# AWN_APP_KEY=

# --- Optional: Email Notifications ---

# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=you@gmail.com
# SMTP_PASSWORD=app-password-here
# SMTP_FROM=you@gmail.com
# NOTIFICATION_EMAIL=you@gmail.com

# --- Optional: CalDAV Calendar Sync ---

# CALDAV_URL=https://caldav.example.com/dav
# CALDAV_USERNAME=user
# CALDAV_PASSWORD=password
# CALDAV_CALENDAR_NAME=Isaac Tasks

# --- Optional: AI Chat ---

# ANTHROPIC_API_KEY=sk-ant-...
# Or for OpenAI:
# OPENAI_API_KEY=sk-...

# --- Docker Settings ---

# HTTPS port to expose the frontend on (default: 443)
ISAAC_PORT=443
